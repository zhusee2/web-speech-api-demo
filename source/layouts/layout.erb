<!doctype html>
<html>
  <head>
    <meta charset="utf-8">

    <!-- Always force latest IE rendering engine or request Chrome Frame -->
    <meta content="IE=edge,chrome=1" http-equiv="X-UA-Compatible">

    <!-- Use title if it's in the page YAML frontmatter -->
    <title><%= current_page.data.title || "WebSpeech API demo by @zhusee2" %></title>

    <%= stylesheet_link_tag "bootstrap-combined.no-icons.min", "all" %>
  </head>

  <body class="<%= page_classes %>">
    <div class="container-fluid">
      <%= yield %>

      <%= partial "footer" %>
    </div>

    <%= javascript_include_tag "libraries", "all" %>
  </body>
</html>